
// This is a placeholder file to resolve TypeScript errors
// In a Lovable app, we don't use Next.js API routes

export function getVapidPublicKey() {
  return { publicKey: 'dummy-key-for-development' };
}
